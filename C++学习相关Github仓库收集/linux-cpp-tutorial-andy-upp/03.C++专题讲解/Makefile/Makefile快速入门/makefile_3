#第三版
#使用变量及模式规则使Makefile更加简洁
#问题 
#1. obj对应的文件需要一个个输入，工作量大，文件数目比较少时还好，文件数目一旦很多的话，obj将很长；
#2. 而且每增加/删除一个文件，都需要修改Makefile。

#变量
obj = main.o fun1.o fun2.o  
target = app3  
CC = gcc

$(target): $(obj)
	$(CC) $(obj) -o $(target)

#模式规则
#所有的.o文件都由对应的.c文件生成
#常用的自动变量
# $<：第一个依赖文件
# $@：目标；
# $^：所有不重复的依赖文件，以空格分开
%.o: %.c
	$(CC) -c $< -o $@
